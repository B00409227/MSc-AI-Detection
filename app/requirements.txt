# Streamlit Cloud requirements — CPU-only, lean build
# Tell pip to find CPU-only PyTorch wheels
--extra-index-url https://download.pytorch.org/whl/cpu

# PyTorch (CPU only — no CUDA needed on Streamlit Cloud)
torch==2.2.2+cpu

# HuggingFace ecosystem
transformers==4.40.1
tokenizers==0.19.1
huggingface-hub==0.22.2
accelerate==0.29.3
sentencepiece==0.2.0

# Data / ML
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2

# NLP
nltk==3.8.1

# Visualisation
matplotlib==3.8.4

# Streamlit
streamlit>=1.33.0

# File upload support
docx2txt>=0.8
python-docx>=0.8.11

# Utilities
tqdm==4.66.2
requests==2.31.0
protobuf>=3.20.0
