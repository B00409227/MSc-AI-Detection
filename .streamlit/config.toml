[server]
maxUploadSize = 50
headless = true

[client]
# Always show full error tracebacks in the UI (even in production/cloud)
showErrorDetails = true

[theme]
primaryColor = "#0066cc"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"
